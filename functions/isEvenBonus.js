function isEvenBonus(string) {
    // Si le paramètre est un string return false
    // Si le paramètre est un string qui contient un nombre (ex: "12") return true

    if (string === String) {
        return true;
    };
};

module.exports = isEvenBonus;